langgraph
langchain
langchain-openai
langfuse
python-dotenv
# Training pipeline (install separately — heavy deps)
# pip install 'unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git'
# pip install --no-deps trl peft accelerate bitsandbytes datasets
